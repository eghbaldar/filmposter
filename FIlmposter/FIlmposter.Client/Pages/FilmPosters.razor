@* @page "/admin/showfilmposter/{category}"
@using Filmposter.Components.Layout
@layout SingleLayout
<AdminNavBar />

@if (Category == "adminNationalFilmPosters")
{
    <PageTitle>پوسترهای فیلم‌های ایرانی</PageTitle>
    <NavLink href="/admin/insertfilmposter/nationalInsertFilmPosters" class="btn btn-primary">درج پوستر</NavLink>
}
else if (Category == "adminForeignFilmPosters")
{
    <PageTitle>پوسترهای فیلم‌های خارجی</PageTitle>
    <NavLink href="/admin/insertfilmposter/foreignInsertFilmPosters" class="btn btn-primary">درج پوستر</NavLink>
}

<div class="card-box">
    <div class="row mt-3">
        <div class="col-12" id="divProfile">

        </div>
    </div>
</div>

@code {
    [Parameter]
    public string? Category { get; set; }
    [Parameter]
    public string? CategoryName { get; set; }
    [CascadingParameter]
    public SingleLayout ParentLayout { get; set; }  // The CascadingParameter of the parent layout

    protected override async Task OnInitializedAsync()
    {
        if (ParentLayout != null)
        {
            switch (Category)
            {
                case "adminNationalFilmPosters":
                    ParentLayout.UpdateTitle("پوستر فیلم‌های ایرانی"); // Update subTitle of the parent layout
                    break;
                case "adminForeignFilmPosters":
                    ParentLayout.UpdateTitle("پوسترهای فیلم‌های خارجی"); // Update subTitle of the parent layout
                    break;
            }
        }
    }
}
 *@