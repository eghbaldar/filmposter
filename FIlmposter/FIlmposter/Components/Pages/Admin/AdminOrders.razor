@* @page "/admin/adminOrders"
@using Filmposter.Components.Layout
@layout SingleLayout
<PageTitle>مدیریت سفارشات</PageTitle>
<AdminNavBar />
@code {
    [CascadingParameter]
    public SingleLayout ParentLayout { get; set; }  // The CascadingParameter of the parent layout

    protected override async Task OnInitializedAsync()
    {
        if (ParentLayout != null) ParentLayout.UpdateTitle("مدیریت سفارشات"); // Update subTitle of the parent layout
    }
}
 *@